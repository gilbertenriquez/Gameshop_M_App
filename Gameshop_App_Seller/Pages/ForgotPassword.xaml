<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Gameshop_App_Seller.Pages.ForgotPassword"
             Title="ForgotPassword" BackgroundColor="#212025">
    
    
    
    <VerticalStackLayout Padding="15"
                     VerticalOptions="FillAndExpand"
                     >
        <Grid RowDefinitions="auto,auto,auto,auto,auto,auto,auto,auto,auto" RowSpacing="50" Margin="30">

            <ImageButton Grid.Row="0" Source="backbtnwhite.png" HeightRequest="20" BackgroundColor="Transparent" HorizontalOptions="Start" x:Name="btnBackImg" Clicked="btnBackImg_Clicked" />

            <Label Grid.Row="1" Text="GAMESHOP" 
                   FontSize="15"
                   FontAttributes="Bold"
                   TextColor="WHITE"
                   HorizontalOptions="Start"/>

            <Label Grid.Row="2" Text="Forgot" 
                   FontSize="30"
                   FontAttributes="Bold"
                   TextColor="WHITE"
                   HorizontalOptions="Start"/>
            
            <Label Grid.Row="3" Text="Password?"
                   TextColor="WHITE"
                   FontAttributes="Bold"
                   FontSize="30"
                   HorizontalOptions="Start" 
                   Margin="0,-30,0,0"
                   />

            <Label Grid.Row="4" Text="Email" Margin="0,0,0,-100"
                   TextColor="WHITE"
                   FontSize="15"
                   HorizontalOptions="Start"/>
            <Frame Grid.Row="5" BackgroundColor="Transparent" BorderColor="WHITE" Padding="0,0">
                <Entry 
                   PlaceholderColor="WHITE"
                   Placeholder="Enter your email"
                   TextColor="WHITE"
                   x:Name="TxtEmail"/>
            </Frame>
            <Button Grid.Row="6" Text="Reset Password"
                TextColor="White"
                    BackgroundColor="#d22f3d"
                    x:Name="BtnSendLink"
                    Clicked="BtnSendLink_Clicked"/>
        </Grid>
    </VerticalStackLayout>

</ContentPage>